<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- In-app updater writes the downloaded APK here and exposes it to the
         system package installer via FileProvider. -->
    <external-files-path name="updates" path="updates/" />
    <cache-path name="update_cache" path="updates/" />
</paths>
